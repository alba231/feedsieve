#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# ↓↓↓ everything below is your existing .git/hooks/commit-msg logic ↓↓↓
commit_msg=$(cat "$1")
if ! echo "$commit_msg" | grep -qE '#[0-9]+'; then
  echo "❌ Commit must reference a ticket, e.g. #123"
  exit 1
fi